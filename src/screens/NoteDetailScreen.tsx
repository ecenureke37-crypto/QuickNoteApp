import React from 'react';
import { View, Text } from 'react-native';

const NoteDetailScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Note Detail Screen</Text>
    </View>
  );
};

export default NoteDetailScreen;